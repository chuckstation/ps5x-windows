# Contributing to ChuckStation 5

Contributions are welcome!

## Guidelines
1. Ensure code conforms to C++20 standard practices.
2. Ensure `cmake --build build` compiles cleanly with zero warnings.
3. Ensure all unit tests pass using `ctest --test-dir build --output-on-failure`.
