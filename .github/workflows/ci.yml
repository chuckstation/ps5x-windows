name: ChuckStation5 CI (Windows)

on:
  push:
    branches: [main, develop]
    tags:     ['v*']
  pull_request:
    branches: [main]

jobs:
  build-release:
    name: Build ChuckStation5 Release (Windows x64)
    runs-on: windows-2022

    steps:
      - uses: actions/checkout@v4

      - name: Configure (MSVC Release)
        run: |
          cmake -B build `
            -G "Visual Studio 17 2022" `
            -A x64 `
            -DCMAKE_BUILD_TYPE=Release `
            -DCHUCKSTATION5_ENABLE_DX12=ON `
            -DCHUCKSTATION5_ENABLE_DX11=ON `
            -DCHUCKSTATION5_ENABLE_CRASH_HANDLER=ON `
            -DCHUCKSTATION5_ENABLE_METRICS=ON `
            -DCHUCKSTATION5_ENABLE_SAVESTATES=ON `
            -DCHUCKSTATION5_ENABLE_INPUT_MAPPING=ON

      - name: Build
        run: cmake --build build --config Release --parallel
